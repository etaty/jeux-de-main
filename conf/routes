# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Home page
GET         /                            controllers.Application.index
GET         /computer-vs-computer        controllers.Application.showComputerVsComputer(rounds : Option[Int] ?= None)
GET         /player-vs-computer          controllers.Application.showPlayerVsComputer
POST        /player-vs-computer          controllers.Application.playPlayerVsComputer
POST        /reset                       controllers.Application.resetPlayerVsComputer

# Map static resources from the /public folder to the /assets URL path
GET         /assets/*file                controllers.Assets.versioned(path="/public", file: Asset)
